%% this is a time consuming bubble sort which sorts the array keeping the key values unchanged
%  this is similar to sorting a dictionary based on the particular key
%  this is quite time consuming as it compares and swapes .
%  updated version of this is each st

function A=sort_array(array)
n=length(array);
for i=1:20
    for j=i+1:n
        if(array(i,1)>=array(j,1))
            temp=array(i,:);
            array(i,:)=array(j,:);
            array(j,:)=temp;
        end
        
    end
    array'
end

A=array;

end